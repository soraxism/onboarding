# ONBS-1991 手動テスト項目書

対象: イントロのチェックマークを付けるタイミングの選択機能

- 設計・仕様: [2026-09-07_admin-ui-and-data-design.md](./2026-09-07_admin-ui-and-data-design.md)
- 現状調査（既存挙動の根拠）: [2026-09-07_current-behavior-survey.md](./2026-09-07_current-behavior-survey.md)

## 前提と準備

| 項目 | 内容 |
|---|---|
| 対象ブランチ | 4 リポジトリすべて `feature/ONBS-1991` |
| 検証ツアー | **ステップが 3 つ以上あるゴールを 2 つ以上**持つツアー（片方は 1 ステップのみのゴールも用意すると境界が見られる） |
| 追加で用意 | ゴール連結を設定したゴール、ステップ分岐を設定したゴール（§5 で使用） |
| ブラウザ | Chrome（拡張機能の検証があるため） |
| 実施方法 | 管理画面・エンドユーザー側・拡張機能とも Claude Code に任せられる。手順は [docs/testing/](../../testing/README.md) |

### 開始前に dev へ反映する（必須）

作業ブランチのままでは dev 環境に載らない。詳細は [CLAUDE.md](../../../CLAUDE.md) の「dev 環境へのデプロイ」。

| 対象 | 状態 |
|---|---|
| 管理画面 / 管理 API | `develop` へマージ済み・デプロイ済み |
| エディタ / プレビュー / ビューワー拡張 | `develop` へ push 済み（ストア申請済み）。**確認にはローカルビルドを使う** |
| エンドユーザー側の配信 JS | **S3 への手動アップロードが必要**（`onboarding-web/build/dev/s3/` の 2 ファイル） |

配信 JS が反映されたかは確認前に実物で見る。0 なら未反映。

```bash
curl -s https://dev-assets.onboarding-app.io/js/onboarding-init.js | grep -c onb_last_step_displayed_goals_
curl -s https://dev-assets.onboarding-app.io/js/onboarding-init-next.js | grep -c onb_last_step_displayed_goals_
```

**拡張機能を確認する前にビルドすること。** 読み込むのはソースではなくビルド成果物なので、
古いビルドのまま確認すると誤判定する。§2 の前に次を通しておく。

```bash
# エディタ拡張
cd Onboarding-Editor-Extension && NODE_ENV=dev npx vite build --mode dev
# プレビュー / ビューワー拡張
cd onboarding-web && npx webpack --config webpack.dev.js && npx webpack --config webpack.dev.js --env product=general
# 疎通確認（8/8 件 OK になること）
cd onboarding && node docs/testing/scripts/check-extensions.mjs
```

**LocalStorage の確認方法**: DevTools → Application → Local Storage。関係するキーは次の 4 つ。

| キー | 意味 |
|---|---|
| `onb_display_goals_{tourID}` | ステップを 1 つ以上表示したゴール |
| `onb_last_step_displayed_goals_{tourID}` | 最終ステップを表示したゴール（**本対応で追加**） |
| `onb_complete_goals_{tourID}` | 終了ボタンを押して完了したゴール |
| `onb_checked_goals_ids_{tourID}` | 顧客スクリプトが `setCheckedGoalIds()` で設定したゴール |

各ケースの開始前に**対象ツアーの上記キーを削除**してから始める（未着手状態を作る）。

---

## 1. 管理画面の設定 UI

| # | 手順 | 期待結果 | 結果 |
|---|---|---|---|
| 1-1 | ツアー編集 → 「表示スタイル設定」→ イントロタブを開く | 「チェックマーク」セクションに背景色と「チェックマークを付けるタイミング」が並ぶ | |
| 1-2 | 選択肢を確認する | 「ゴールの表示（いずれかのステップが表示）」「ゴールの完了（最後のステップが表示）」の 2 つ。カードが縦に並び、文言が枠内で読める | |
| 1-3 | この設定を一度も保存していないツアーで開く | 「ゴールの表示」が選択されている | |
| 1-4 | 「ゴールの完了」を選び保存 → モーダルを閉じて再度開く | 「ゴールの完了」が選択された状態で開く | |
| 1-5 | 「デフォルトに戻す」を押す | **背景色だけ**が既定色（#46a6ff）に戻り、タイミングの選択は変わらない | |
| 1-6 | 背景色を変更して保存 → 再度開く | 背景色とタイミングの両方が保存されている（片方の保存で他方が消えない） | |
| 1-7 | 保存後にブラウザをリロードして開く | 選択が保持されている | |

## 2. エディター拡張機能の設定 UI

| # | 手順 | 期待結果 | 結果 |
|---|---|---|---|
| 2-1 | 拡張機能でツアーを開き、イントロのゴール一覧を選択する | インラインメニューに「チェック色」と「チェックのタイミング」が並ぶ | |
| 2-2 | 「チェックのタイミング」を押す | プルダウンが開き、2 つの選択肢が全文表示される | |
| 2-3 | 「ゴールの完了（最後のステップが表示）」を選ぶ | ボタンの表示が「ゴールの完了」に変わり、保存される（保存状態の表示を確認） | |
| 2-4 | 管理画面で同じツアーの表示スタイル設定を開く | 「ゴールの完了」が選択されている（拡張機能の変更が管理画面に反映される） | |
| 2-5 | **管理画面で「ゴールの完了」を保存 → 拡張機能でチェック色だけを変更 → 管理画面を再度開く** | **タイミングが「ゴールの完了」のまま残っている**（色の変更でタイミングが消えない） | |
| 2-6 | 逆順（拡張機能でタイミング変更 → 管理画面で背景色だけ保存 → 拡張機能で確認） | タイミングが残っている | |

> 2-5 / 2-6 は今回いちばん壊れやすい箇所（拡張機能の API が `styles.intro` を丸ごと置き換えていた問題の修正確認）。

## 3. エンドユーザー側 — 既定（ゴールの表示）

設定を「ゴールの表示」にして公開したツアーで確認する。**従来と同じ挙動になること**が目的。

| # | 手順 | 期待結果 | 結果 |
|---|---|---|---|
| 3-1 | ランチャー → イントロを開く | 未着手のゴールにチェックが付いていない | |
| 3-2 | ゴールを開いて 1 ステップ目を表示し、× で閉じる | `onb_display_goals_` にゴールIDが入る | |
| 3-3 | イントロを再度開く | そのゴールに**チェックが付いている** | |
| 3-4 | 最後まで進めて「終了」を押す | `onb_complete_goals_` にゴールIDが入る。チェックは付いたまま | |
| 3-5 | ランチャーの未完了バッジ | 3-2 の時点で 1 件減る（従来どおり） | |
| 3-6 | 最終ステップを表示した時点で `onb_last_step_displayed_goals_` を確認 | **設定が「ゴールの表示」でもゴールIDが記録されている**（設定切替時の引き継ぎ用） | |

## 4. エンドユーザー側 — ゴールの完了

設定を「ゴールの完了」にして公開したツアーで確認する。

| # | 手順 | 期待結果 | 結果 |
|---|---|---|---|
| 4-1 | イントロを開く（未着手） | チェックが付いていない | |
| 4-2 | ゴールを開いて 1 ステップ目を表示し、× で閉じる → イントロを開く | **チェックが付かない**（`onb_display_goals_` には入るが判定に使われない） | |
| 4-3 | 途中のステップまで進めて × で閉じる → イントロを開く | チェックが付かない | |
| 4-4 | 最終ステップまで進める（「終了」は押さない） | `onb_last_step_displayed_goals_` にゴールIDが入る | |
| 4-5 | 4-4 の状態で**ツアーを進めたまま**イントロを開く | そのゴールに**チェックが付いている**（進行中の即時反映） | |
| 4-6 | 4-4 の後に × で閉じて、イントロを開く | チェックが付いている | |
| 4-7 | 最終ステップで「終了」を押す → イントロを開く | チェックが付いている（`onb_complete_goals_` にも入る） | |
| 4-8 | ランチャーの未完了バッジ | 4-4 の時点で 1 件減る（4-2 の時点では減らない） | |
| 4-9 | ステップが 1 つだけのゴールを表示する | 表示した時点でチェックが付く（1 ステップ目 = 最終ステップ） | |
| 4-10 | チェックマークの背景色を設定している場合、4-5 で付いたチェックの色 | イントロを開いたときに設定した色になっている | |

## 5. ゴール連結・ステップ分岐

| # | 手順 | 期待結果 | 結果 |
|---|---|---|---|
| 5-1 | 「ゴールの完了」設定で、**ゴール連結**のあるゴールの最終ステップを表示する | チェックが付く（連結先へ進む前に付く。**仕様として許容**） | |
| 5-2 | 5-1 の連結先ゴールを最後まで表示する | 連結先ゴールにもチェックが付く | |
| 5-3 | 「ゴールの完了」設定で、**ステップ分岐**により最終ステップを通らないルートを進む | チェックが付かない（レポートの完了集計と同じ基準） | |

## 6. 設定切替時の引き継ぎ（重要）

「ゴールの表示」で運用していたツアーを「ゴールの完了」へ切り替えたときの挙動。
**LocalStorage を消さずに**設定だけを切り替えて確認する。

| # | 事前状態（設定＝ゴールの表示で操作） | 切替後の期待結果 | 結果 |
|---|---|---|---|
| 6-1 | ゴールを最後まで進めて「終了」を押した | **チェックが維持される**（`onb_complete_goals_` に残っているため） | |
| 6-2 | 最終ステップを表示して × で閉じた（今回のリリース後に操作） | チェックが維持される（`onb_last_step_displayed_goals_` に記録済み） | |
| 6-3 | 1 ステップ目だけ表示して閉じた | **チェックが外れる**（設定変更の意図どおり） | |
| 6-4 | 6-3 の状態から設定を「ゴールの表示」へ戻す | チェックが再度付く（`onb_display_goals_` は残り続けるため戻せる） | |

## 7. 顧客カスタム・公開 API への影響（回帰確認）

| # | 手順 | 期待結果 | 結果 |
|---|---|---|---|
| 7-1 | 自動表示条件「ゴール利用状況」の**完了済み / 未完了**を設定したゴール | 判定が従来どおり（終了ボタン押下が基準。タイミング設定の影響を受けない） | |
| 7-2 | 自動表示条件「ゴール利用状況」の**表示済み / 未表示** | 判定が従来どおり | |
| 7-3 | `STANDSMotion.getIncompleteGoalIds()` をコンソールで実行 | 返り値が従来どおり（`onb_complete_goals_` 基準） | |
| 7-4 | `STANDSMotion.setCheckedGoalIds([...])` を実行 → イントロを開く | 指定したゴールだけにチェックが付く（**タイミング設定より優先される**） | |
| 7-5 | `STANDSMotion.clearCheckedGoalIds()` を実行 → イントロを開く | タイミング設定に応じた判定に戻る | |
| 7-6 | カスタム JS を持つ顧客プロダクト（`prod/65`・`7738`・`722` 等）で、設定を既定のままツアーを実行 | 従来どおりの表示。コンソールにエラーが出ない | |

## 8. データ整合・異常系

| # | 手順 | 期待結果 | 結果 |
|---|---|---|---|
| 8-1 | `onb_last_step_displayed_goals_{tourID}` に不正な文字列（例 `not-json`）を入れてツアーを実行 | エラーで止まらず、値が作り直される | |
| 8-2 | ゴールを削除したツアーを公開 → 削除前に到達していた端末で開く | `onb_last_step_displayed_goals_` から削除済みゴールIDが除去される（`onb_complete_goals_` と同じ整合） | |
| 8-3 | ツアーを**複製**する | 複製先にもタイミング設定が引き継がれる | |
| 8-4 | 新規ツアーを作成する | タイミングが「ゴールの表示」（テンプレート既定値） | |
| 8-5 | この設定より前に作られた既存ツアー（キーなし）を公開して実行 | 従来どおりゴールの表示時にチェックが付く | |

## 9. 配信経路（新旧 JS の両方）

`use_refactored_onboarding_init` フラグで配信される JS が変わるため、**両方**で確認する。

| # | 対象 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|
| 9-1 | 新側（next） | フラグ有効なプロダクト、または URL に `?onbd_init_ver=next` を付けて開く | §3・§4 の挙動が成立する | |
| 9-2 | 旧側（legacy） | フラグ無効なプロダクト、または `?onbd_init_ver=legacy` で開く | §3・§4 の挙動が成立する | |
| 9-3 | プレビュー | 管理画面のプレビューでツアーを実行 | 設定に応じた挙動になる | |
| 9-4 | ビューワー拡張 | ビューワーでツアーを実行 | 設定に応じた挙動になる | |

## 10. レポートとの整合

| # | 手順 | 期待結果 | 結果 |
|---|---|---|---|
| 10-1 | 「ゴールの完了」設定で最終ステップまで到達（終了ボタンは押さない）→ 翌日レポートを確認 | レポートの完了 UU に計上される（イントロのチェックと基準が一致する） | |
| 10-2 | 設定を「ゴールの表示」に変えて同じ操作 | レポートの数値の出方は 10-1 と変わらない（**設定はレポートに影響しない**） | |

---

## 自動テストで担保済みの範囲（手動確認は不要）

| 内容 | 担保しているテスト |
|---|---|
| 設定の読み取り（未設定・未知の値は既定へ倒す） | `onboarding-web` ユニット `GoalRepository.test.ts` |
| 最終ステップ表示の記録（境界・重複・壊れた値） | 同上 |
| チェック判定の切り替え（既定 / 完了時 / 論理和） | `onboarding-web` ユニット `onboarding-init.test.ts` + `onboarding-e2e-test` の `goalCheckmarkTiming` |
| 削除済みゴールIDの掃除 / 壊れた値でランチャーが出ること | `onboarding-web` ユニット `onboarding-init.test.ts` |
| 既定設定で DOM 操作が走らないこと | `onboarding-web` ユニット `stands.onbd.test.ts` |
| 記録が失敗しても表示トラッキングが落ちないこと | 同上 |
| 進行中のイントロDOM更新 | `onboarding-web` ユニット `TourIntro.test.ts` |
| 実際にゴールを完了したときの新キー記録 | `onboarding-e2e-test` の `goalCompleted` |
| 管理画面の設定UI（既定値・保存・再読込） | `onboarding-manage-web` の spec + Storybook |

**E2E の制約**: `onboarding-e2e-test` の legacy モードは dev 環境に配信済みの JS を使うため、
ローカルの変更が反映されない（next モードのみローカルビルドを差し込める）。
**旧側（`src/js/`）の挙動は §9-2 の手動確認が唯一の検証経路**なので必ず実施すること。

## 補足: 確認しなくてよいこと

- **レポートの完了数の変化** — レポートは元から「最終ステップの表示」で集計しており、今回の変更で数え方は変わらない
- **`onb_complete_goals_` の書き込みタイミング** — 変更していない（終了ボタン押下のまま）
- **ポップアップの挙動** — ポップアップはイントロに出ないため表示上の変化はない
  （バッジの数え方は従来どおりポップアップも含む）
